{
  "name": "kurva-bezier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "setup-env": "pip install virtualenv && virtualenv app/api/venv && cd app/api/venv/Scripts && activate.bat && cd ../../../.. && pip install -r app/api/requirements.txt",
    "setup": "npm install && npm run setup-env",
    "run-app": "run.bat"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.1.3"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "eslint": "^8",
    "eslint-config-next": "14.1.3"
  }
}
